# Solana / Anchor Python client
anchorpy>=0.20.0
solders>=0.21.0
solana>=0.34.0

# BCB data (python-bcb is already in the repo root requirements.txt,
# listed here for standalone relayer installs)
python-bcb==0.3.0
pandas>=2.0.0
numpy>=1.26.0
