interface:
  display_name: "Iliquid Nightly"
  short_description: "Repo-specific patterns and workflows for iliquid_nightly"
  default_prompt: "Use the iliquid_nightly repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true