# Overnight run — 2026-08-27 → 28

Autonomous session under explicit merge-and-deploy authority. Every merge went
through the new PR-time SQL-compile gate; every production change was verified
against live Supabase after its dispatch.

## Shipped (merged + deployed)

| PR      | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Deployed                                                                       |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| #123    | **sql-compile CI gate**: every PR now builds schema + 25 migrations + the full analytical layer + all dashboard sources on a throwaway Postgres (empty-DB smoke guards downgraded only under `silo.ci_smoke_bypass`). Closed SERVING step 2.                                                                                                                                                                                                                                                                                                                                                         | CI only                                                                        |
| #124    | **B3 typing v2** (migration 23): tpmerc 012/013 relabeled option exercises, 017 = auction; `fund_type` (etf/fii/fidc/fiagro from CODBDI, validated vs `cvm_etf_registry`); `share_class`/`governance_segment` from ESPECI (ISIN-cross-checked, zero disagreements); `underlying_ticker` on option endpoints via the published ISIN mapping (14,895/14,900 matched, 1:1); `option_exercises` + `auctions` endpoints; PTOEXE/INDOPC/DISMES exposed; migration 20 replay guard (its unguarded replay would have failed every nightly bootstrap after 23). Catalog v7.                                   | run #179 ✔                                                                     |
| #125    | **Completeness + honest API**: `mv_period_completeness` + `latest_complete_period()` (month ended AND ≥80% of trailing-6 median fund count; validated per-case incl. FIP's early-filed year-end row); `api.panel`/`fund_nav` default windows clamp per family (explicit `p_to` = escape hatch); `coverage()` gains `complete_through` + `funds_<family>` rows — the FIP as-of question answered as designed; daily `close_return` guarded (prev session ≤7d, unchanged fatcot); lookup hardened (ILIKE escape, rank-before-limit, pg_trgm via migration 24). Catalog v8. Closed SERVING steps 4 + 5. | run #179 ✔ — verified live: lcp fi=2026-07-01, fidc=2026-07-31, fip=2025-12-31 |
| #126    | **FCA ticker bridge** (migration 25): `cia_ticker` + `vw_company_ticker` from CVM's published FCA valores-mobiliários file (963/963 rows keyable live; Petrobras → PETR3 ON + PETR4 PN); wired into daily + backfill; `lookup` company rows carry `tickers` — the published CNPJ↔ticker map, zero name matching. Catalog v9.                                                                                                                                                                                                                                                                         | data arrives with the 06:00 daily                                              |
| #127    | **silo-client Python SDK** (`sdk/`): catalog-driven, honest-window defaults, NaN-stays-NaN, server errors surfaced verbatim; 9 offline tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                        | n/a (library)                                                                  |
| (P4 PR) | **Dashboard**: ~20 sources clamped to complete periods; `/fund` FIP-anchor bug fixed (right ~5 months were empty by construction); FIDC delinquency x-axis alignment (month-end→first-of-month); **Focus fix** — `ExpectativaMercadoMensais` singular (78,941 rows the plural could never match) + January horizon-rollover explained on the page; new `/markets` page over the 2019-2026 B3 tape; `performance.md` stale text replaced + charts; `etf.md` gains B3-tape series.                                                                                                                     | merged after run #179; Vercel rebuild                                          |

Also: full docs pass — new `api-docs/cotahist-dictionary.mdx` (register-01 fields,
TPMERC/CODBDI/ESPECI tables, fatcot semantics), every affected api-docs page
updated, `.mintlify/Assistant.md` new rules, `docs/API.md` PostgREST resource
table, SERVING.md status table rewritten (steps 2–6 done, 7 obsoleted by the
Supabase-native decision), CHANGELOG brought current (was two catalog versions
behind).

## Measured overnight (read-only, production)

- **fatcot**: 2,101,288 cash rows at factor 1 vs 4,503 across 13 tickers at
  other factors; only GOLL2 (1000→1) and IBOV11 (100→1) ever flipped. → guard +
  document, no price rescaling.
- **Jump screen** (2025+ cash tape): 315 jumps >40% across 151 codnegs — 0 from
  fatcot, 2 from ISIN changes, 28 after 30d+ gaps (now nulled by the adjacency
  guard), ~285 corporate-action/real-move candidates. That is the size of the
  splits problem.
- **B3 corporate-events source check**: the b3.com.br proxy answers a bare
  client 200-empty (browser session required), per-company, no bulk file; rb3
  has no events template either → `b3_corporate_event` stays a spec
  (INSTRUMENTS.md), no ingest under integrity rule 1.
- **tpmerc 021**: codbdi 93, `Q`-suffixed tickers — odd-lot assumption now
  documented with evidence.

## Your queue (nothing blocking, in value order)

1. **Historical FCA years**: dispatch `backfill.yml` with `entity=cia_aberta`
   once — the yearly FCA files land alongside IPE. (Current year arrives daily
   on its own.)
2. **Key rotation**: the shared publishable key is committed in the docs and
   marked testing-only; go-live still needs per-user keys + RLS
   (`docs/security/enable_rls.sql` is ready).
3. **Corporate actions**: decide whether a browser-session scraper for B3's
   events proxy is worth building (the spec + in-house signals are ready;
   ~285 candidate jumps say the problem is real but bounded).
4. Phases B/C/D (futures settlement, curves, index composition) remain open in
   INSTRUMENTS.md — untouched tonight by design.

## Deviations / judgment calls

- Securitizadora charts clamp to "last ended month" (they live outside
  `fact_fund_monthly`, so no coverage-based completeness exists for them yet).
- `ops` page deliberately NOT clamped — its job is showing the partial month.
- Two catalog bumps per batch (7, 8, 9) instead of one big one: each merged PR
  carries the version its change introduced, so the version history stays
  honest.
