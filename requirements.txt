# Brazilian Financial Data pipeline — fetch / parse / store only
# Single root requirements.txt. The historical per-service requirements
# files were removed when the FastAPI services were deleted.

# HTTP / network (CVM + ANBIMA)
aiohttp==3.14.1
aiofiles==23.2.1
dnspython==2.6.1
httpx>=0.27.0

# Data parsing
pandas==2.2.3
numpy==1.26.3
python-dateutil==2.8.2
lxml==6.1.0
openpyxl>=3.1.0

# BACEN client
python-bcb==0.3.0
requests==2.33.0

# Storage
psycopg2-binary>=2.9

# Config / runtime
python-dotenv==1.2.2

# Read API (Flask) — `python -m serve.app` over schema api; not an ingest trigger
flask>=3.0,<4.0

# Testing
pytest==9.0.3
pytest-asyncio==1.4.0
hypothesis==6.98.3

# Local dev / offline verification — used by scripts/seed_local_db.py and
# scripts/run_analysis_local.py to seed and query a local DuckDB file (no
# Supabase credentials required). Not used by the production pipeline.
duckdb>=1.0
