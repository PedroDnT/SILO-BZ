# TODO

## Done — pipeline consolidation
- [x] Drop FastAPI services + gateway + Mintlify docs + Solana oracle.
- [x] Restructure src/ into fetchers/, parsers/, store/, pipeline/.
- [x] Cherry-pick FI/FII entities + redesigned schema with audit log.
- [x] Bump aiohttp 3.13.4, requests 2.33.0, lxml 6.1.0, python-dotenv 1.2.2.

## Pending — operations
- [ ] Apply `src/store/schema.sql` against the target Supabase project.
- [ ] Run initial historical backfill: `python -m src.pipeline.run_backfill --start-year 2019`.
- [ ] Set GitHub Actions secrets `SUPABASE_URL`, `SUPABASE_SERVICE_KEY` so the
      `daily_ingest.yml` cron starts succeeding.

## Future scope
- [ ] B3 fetcher — drop in `src/fetchers/b3_fetcher.py` + extend `src/store/schema.sql`
      once a live B3 endpoint is validated. Previous proxy hit a non-B3 domain
      and only returned hardcoded samples; removed for honesty.
- [ ] ANBIMA — needs paid API credentials.
