---
phase: 01-db-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - src/db/Dockerfile
  - src/db/requirements.txt
  - src/db/alembic.ini
  - src/db/alembic/env.py
  - src/db/alembic/script.py.mako
  - src/db/alembic/versions/.gitkeep
  - src/db/init.sql
  - .env.example
autonomous: true
requirements:
  - INFRA-01
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "docker-compose up starts a PostgreSQL 16 container that passes pg_isready healthcheck"
    - "db_migrate one-shot service runs alembic upgrade head and exits 0 before any API service starts"
    - "All three API services declare depends_on db_migrate with service_completed_successfully condition"
    - "All database credentials and DATABASE_URL are read from environment variables — no hardcoded connection strings in any source file"
    - "src/db/init.sql creates three schemas (cvm, bacen, b3_calc) and three per-service users with correct GRANT statements"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Postgres 16 service + db_migrate one-shot + API service dependency chain"
      contains: "pg_isready"
    - path: "src/db/Dockerfile"
      provides: "db_migrate container image (runs alembic upgrade head)"
      contains: "alembic upgrade head"
    - path: "src/db/alembic/env.py"
      provides: "Async Alembic migration runner with include_schemas=True"
      contains: "include_schemas=True"
    - path: "src/db/init.sql"
      provides: "Schema creation + per-service user grants"
      contains: "CREATE SCHEMA IF NOT EXISTS cvm"
  key_links:
    - from: "docker-compose.yml db service"
      to: "src/db/init.sql"
      via: "volume mount /docker-entrypoint-initdb.d/01-init.sql:ro"
      pattern: "docker-entrypoint-initdb"
    - from: "docker-compose.yml db_migrate service"
      to: "src/db/alembic/env.py"
      via: "CMD alembic -c src/db/alembic.ini upgrade head inside container"
      pattern: "alembic upgrade head"
    - from: "docker-compose.yml cvm_api/bacen_api/b3_calc_api"
      to: "db_migrate"
      via: "depends_on: db_migrate: condition: service_completed_successfully"
      pattern: "service_completed_successfully"
---

<objective>
Establish the PostgreSQL 16 container, Docker Compose startup ordering, init.sql schema/user creation, and Alembic async migration infrastructure.

Purpose: All subsequent phases depend on this foundation. The three-tier dependency chain (db → db_migrate → API services) ensures migrations always run before any API service starts, with no manual steps required.
Output: A working docker-compose.yml with db + db_migrate + all API services, src/db/init.sql, src/db/Dockerfile for the migration runner, and Alembic async env.py configuration.
</objective>

<execution_context>
@/Users/pedrotodescan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedrotodescan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db-foundation/01-RESEARCH.md
@docker-compose.yml
@src/cvm_api/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: PostgreSQL 16 container + init.sql schema/user creation</name>
  <files>
    src/db/init.sql
    src/db/requirements.txt
    .env.example
  </files>
  <action>
Create `src/db/init.sql` — this file is mounted to `/docker-entrypoint-initdb.d/01-init.sql:ro` inside the postgres container and runs automatically on first volume initialization:

```sql
-- Creates three isolated schemas and their per-service users
-- Alembic runs as the postgres superuser; per-service users only need runtime access

CREATE SCHEMA IF NOT EXISTS cvm;
CREATE SCHEMA IF NOT EXISTS bacen;
CREATE SCHEMA IF NOT EXISTS b3_calc;

CREATE USER IF NOT EXISTS cvm_user WITH PASSWORD 'cvm_secret';
CREATE USER IF NOT EXISTS bacen_user WITH PASSWORD 'bacen_secret';
CREATE USER IF NOT EXISTS b3_calc_user WITH PASSWORD 'b3_secret';

-- Grant schema usage and table access (ALTER DEFAULT PRIVILEGES covers future tables too)
GRANT USAGE ON SCHEMA cvm TO cvm_user;
GRANT ALL ON ALL TABLES IN SCHEMA cvm TO cvm_user;
ALTER DEFAULT PRIVILEGES IN SCHEMA cvm GRANT ALL ON TABLES TO cvm_user;

GRANT USAGE ON SCHEMA bacen TO bacen_user;
GRANT ALL ON ALL TABLES IN SCHEMA bacen TO bacen_user;
ALTER DEFAULT PRIVILEGES IN SCHEMA bacen GRANT ALL ON TABLES TO bacen_user;

GRANT USAGE ON SCHEMA b3_calc TO b3_calc_user;
GRANT ALL ON ALL TABLES IN SCHEMA b3_calc TO b3_calc_user;
ALTER DEFAULT PRIVILEGES IN SCHEMA b3_calc GRANT ALL ON TABLES TO b3_calc_user;
```

Create `src/db/requirements.txt` for the db_migrate container:
```
sqlalchemy[asyncio]==2.0.48
asyncpg==0.31.0
alembic==1.18.4
python-dotenv==1.0.1
```

Update `.env.example` (append to existing file, do not remove any existing entries) with:
```
# PostgreSQL (DB Foundation — Phase 1)
POSTGRES_PASSWORD=changeme_in_production
DATABASE_URL=postgresql+asyncpg://postgres:changeme_in_production@db:5432/iliquid
CVM_DATABASE_URL=postgresql+asyncpg://cvm_user:cvm_secret@db:5432/iliquid
BACEN_DATABASE_URL=postgresql+asyncpg://bacen_user:bacen_secret@db:5432/iliquid
B3_CALC_DATABASE_URL=postgresql+asyncpg://b3_calc_user:b3_secret@db:5432/iliquid
```

Note: `CREATE USER IF NOT EXISTS` requires PostgreSQL 9.6+; the `postgres:16` image satisfies this.
  </action>
  <verify>
    <automated>grep -q "CREATE SCHEMA IF NOT EXISTS cvm" /Users/pedrotodescan/Documents/Dev/iliquid_nightly/src/db/init.sql && grep -q "ALTER DEFAULT PRIVILEGES" /Users/pedrotodescan/Documents/Dev/iliquid_nightly/src/db/init.sql && grep -q "DATABASE_URL" /Users/pedrotodescan/Documents/Dev/iliquid_nightly/.env.example && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>src/db/init.sql exists with all three schemas and three users; .env.example includes all five DB env vars; src/db/requirements.txt exists with the four required packages.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic async migration setup + db_migrate Dockerfile</name>
  <files>
    src/db/Dockerfile
    src/db/alembic.ini
    src/db/alembic/env.py
    src/db/alembic/script.py.mako
    src/db/alembic/versions/.gitkeep
  </files>
  <action>
Create the full Alembic async setup for the db_migrate one-shot service.

Note: `src/db/__init__.py` and `src/db/models/__init__.py` are NOT created here — Plan 01-02 owns all files under src/db/models/ and creates both package init files as part of the models package setup. Alembic env.py references `from src.db.models.base import Base` which will exist after Plan 01-02 executes.

**src/db/Dockerfile** — one-shot migration runner:
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY src/db/requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r requirements.txt
# Copy full src/ so alembic env.py can import src.db.models
COPY src/ ./src/
# alembic.ini lives at src/db/alembic.ini; run from /app so src.db.models is importable
CMD ["alembic", "-c", "src/db/alembic.ini", "upgrade", "head"]
```

**src/db/alembic.ini** — minimal configuration (DATABASE_URL read from env in env.py):
```ini
[alembic]
script_location = src/db/alembic
prepend_sys_path = .
[loggers]
keys = root,sqlalchemy,alembic
[handlers]
keys = console
[formatters]
keys = generic
[logger_root]
level = WARN
handlers = console
qualname =
[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine
[logger_alembic]
level = INFO
handlers =
qualname = alembic
[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic
[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
```

**src/db/alembic/env.py** — async runner with multi-schema support:
```python
import asyncio
import os
from logging.config import fileConfig

from sqlalchemy.ext.asyncio import async_engine_from_config
from sqlalchemy import pool
from alembic import context

# Load .env if present (local dev; Docker injects env vars directly)
try:
    from dotenv import load_dotenv
    load_dotenv()
except ImportError:
    pass

# Import ALL models so autogenerate captures all three schemas
from src.db.models.base import Base
from src.db.models import cvm, bacen, b3_calc  # noqa: F401

config = context.config

if config.config_file_name is not None:
    fileConfig(config.config_file_name)

target_metadata = Base.metadata

# Read DATABASE_URL from environment — no hardcoded strings (INFRA-05)
database_url = os.environ["DATABASE_URL"]
# Ensure asyncpg dialect
if database_url.startswith("postgresql://"):
    database_url = database_url.replace("postgresql://", "postgresql+asyncpg://", 1)
config.set_main_option("sqlalchemy.url", database_url)


def do_run_migrations(connection):
    context.configure(
        connection=connection,
        target_metadata=target_metadata,
        include_schemas=True,                # CRITICAL: detects non-public schemas
        version_table_schema="public",       # alembic_version stays in public schema
    )
    with context.begin_transaction():
        context.run_migrations()


async def run_async_migrations():
    connectable = async_engine_from_config(
        config.get_section(config.config_ini_section),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,  # one-shot: no connection reuse
    )
    async with connectable.connect() as connection:
        await connection.run_sync(do_run_migrations)
    await connectable.dispose()


def run_migrations_online():
    asyncio.run(run_async_migrations())


def run_migrations_offline():
    # Offline mode not needed for this project; raise to avoid silent no-op
    raise NotImplementedError("Offline migration mode is not supported in this project")


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```

**src/db/alembic/script.py.mako** — standard Alembic migration template:
```mako
"""${message}

Revision ID: ${up_revision}
Revises: ${down_revision | comma,n}
Create Date: ${create_date}

"""
from typing import Sequence, Union

from alembic import op
import sqlalchemy as sa
${imports if imports else ""}

# revision identifiers, used by Alembic.
revision: str = ${repr(up_revision)}
down_revision: Union[str, None] = ${repr(down_revision)}
branch_labels: Union[str, Sequence[str], None] = ${repr(branch_labels)}
depends_on: Union[str, Sequence[str], None] = ${repr(depends_on)}


def upgrade() -> None:
    ${upgrades if upgrades else "pass"}


def downgrade() -> None:
    ${downgrades if downgrades else "pass"}
```

Create `src/db/alembic/versions/.gitkeep` as an empty file (keeps the versions directory tracked in git before any migrations are generated).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('/Users/pedrotodescan/Documents/Dev/iliquid_nightly/src/db/alembic/env.py').read()); print('SYNTAX_OK')" && grep -q "include_schemas=True" /Users/pedrotodescan/Documents/Dev/iliquid_nightly/src/db/alembic/env.py && grep -q "version_table_schema" /Users/pedrotodescan/Documents/Dev/iliquid_nightly/src/db/alembic/env.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>src/db/Dockerfile, src/db/alembic.ini, src/db/alembic/env.py, src/db/alembic/script.py.mako all exist; env.py passes Python syntax check; include_schemas=True and version_table_schema="public" are present in env.py. src/db/__init__.py and src/db/models/__init__.py are NOT created here — Plan 01-02 owns them.</done>
</task>

<task type="auto">
  <name>Task 3: Update docker-compose.yml with full DB topology</name>
  <files>
    docker-compose.yml
  </files>
  <action>
Rewrite `docker-compose.yml` to add the three-tier dependency chain while preserving all existing service definitions (cvm_api, b3_calc_api, bacen_api, docs).

The new compose file must:
1. Add `db` service (postgres:16) with pg_isready healthcheck, init.sql mount, and `postgres -N 200` command (sets max_connections=200 per QUERY-04)
2. Add `db_migrate` one-shot service (no restart) that depends on `db: condition: service_healthy`
3. Update all three API services to:
   - Add `depends_on: db_migrate: condition: service_completed_successfully` AND `db: condition: service_healthy`
   - Add their per-service `*_DATABASE_URL` environment variable
4. Add `postgres_data` named volume
5. Keep all existing ports, volumes (cache, temp), healthchecks, and restart policies unchanged

Full replacement docker-compose.yml:
```yaml
version: "3.9"

services:

  db:
    image: postgres:16
    container_name: iliquid_db
    command: postgres -N 200
    environment:
      POSTGRES_DB: iliquid
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./src/db/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d iliquid"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - br_finance

  db_migrate:
    build:
      context: .
      dockerfile: src/db/Dockerfile
    container_name: iliquid_db_migrate
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://postgres:changeme_in_production@db:5432/iliquid}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - br_finance
    # No restart — this is a one-shot job; service_completed_successfully requires exit 0

  cvm_api:
    build:
      context: .
      dockerfile: src/cvm_api/Dockerfile
    image: cvm-api:latest
    container_name: cvm_api
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=info
      - CVM_DATABASE_URL=${CVM_DATABASE_URL:-postgresql+asyncpg://cvm_user:cvm_secret@db:5432/iliquid}
    volumes:
      - ./cache:/app/cache
      - ./temp:/app/temp
    depends_on:
      db_migrate:
        condition: service_completed_successfully
      db:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - br_finance

  b3_calc_api:
    build:
      context: .
      dockerfile: src/b3_calc_api/Dockerfile
    image: b3-calc-api:latest
    container_name: b3_calc_api
    ports:
      - "8001:8001"
    environment:
      - LOG_LEVEL=info
      - B3_CALC_DATABASE_URL=${B3_CALC_DATABASE_URL:-postgresql+asyncpg://b3_calc_user:b3_secret@db:5432/iliquid}
    depends_on:
      db_migrate:
        condition: service_completed_successfully
      db:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - br_finance

  bacen_api:
    build:
      context: .
      dockerfile: src/bacen_api/Dockerfile
    image: bacen-api:latest
    container_name: bacen_api
    ports:
      - "8002:8002"
    environment:
      - BACEN_API_PORT=8002
      - LOG_LEVEL=info
      - BACEN_DATABASE_URL=${BACEN_DATABASE_URL:-postgresql+asyncpg://bacen_user:bacen_secret@db:5432/iliquid}
    depends_on:
      db_migrate:
        condition: service_completed_successfully
      db:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - br_finance

  docs:
    build:
      context: .
      dockerfile: docs/Dockerfile
    image: br-finance-docs:latest
    container_name: br_finance_docs
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    depends_on:
      - cvm_api
      - b3_calc_api
      - bacen_api
    restart: unless-stopped
    networks:
      - br_finance

networks:
  br_finance:
    driver: bridge

volumes:
  postgres_data:
```

Note: `${VAR:-default}` syntax provides fallback defaults so the file works without a .env file for development, while production overrides via environment variables (INFRA-05 compliant — the default is visible but no secret is hardcoded as a constant).
  </action>
  <verify>
    <automated>python3 -c "
import yaml, sys
with open('/Users/pedrotodescan/Documents/Dev/iliquid_nightly/docker-compose.yml') as f:
    c = yaml.safe_load(f)
services = c['services']
checks = [
    'db' in services,
    'db_migrate' in services,
    services['db'].get('healthcheck') is not None,
    'service_completed_successfully' in str(services['cvm_api'].get('depends_on', {})),
    'service_completed_successfully' in str(services['bacen_api'].get('depends_on', {})),
    'service_completed_successfully' in str(services['b3_calc_api'].get('depends_on', {})),
    'postgres_data' in c.get('volumes', {}),
    'CVM_DATABASE_URL' in str(services['cvm_api'].get('environment', [])),
    'BACEN_DATABASE_URL' in str(services['bacen_api'].get('environment', [])),
    'B3_CALC_DATABASE_URL' in str(services['b3_calc_api'].get('environment', [])),
]
failed = [i for i, v in enumerate(checks) if not v]
print('PASS' if not failed else f'FAIL checks {failed}')
" 2>&1</automated>
  </verify>
  <done>docker-compose.yml contains: db service with pg_isready healthcheck, db_migrate one-shot with no restart, all three API services with service_completed_successfully dependency on db_migrate, per-service DATABASE_URL env vars, and postgres_data named volume.</done>
</task>

</tasks>

<verification>
1. `grep -q "include_schemas=True" src/db/alembic/env.py` passes
2. `grep -q "service_completed_successfully" docker-compose.yml` passes
3. `grep -q "CREATE SCHEMA IF NOT EXISTS cvm" src/db/init.sql` passes
4. `grep -q "pg_isready" docker-compose.yml` passes
5. `grep -q "DATABASE_URL" .env.example` passes
6. No connection string literals appear in Python source files: `grep -r "postgresql://" src/cvm_api/ src/bacen_api/ src/b3_calc_api/` returns no output
7. `src/db/__init__.py` and `src/db/models/__init__.py` are NOT created by this plan (owned by Plan 01-02)
</verification>

<success_criteria>
- src/db/init.sql creates three schemas and three per-service users with correct GRANT statements
- src/db/alembic/env.py is syntactically valid Python with include_schemas=True and version_table_schema="public"
- src/db/Dockerfile can be built with `docker build -f src/db/Dockerfile .`
- docker-compose.yml YAML is valid and contains the three-tier dependency chain
- All *_DATABASE_URL env vars use environment variable substitution, not hardcoded connection strings
- .env.example documents all five database env vars
</success_criteria>

<output>
After completion, create `.planning/phases/01-db-foundation/01-01-SUMMARY.md`
</output>
